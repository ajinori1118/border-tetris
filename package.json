{
  "name": "border-tetris",
  "version": "1.0.0",
  "description": "Server-side core logic prototype for a border-wrapping multiplayer tetris game",
  "private": true,
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test --test-isolation=none dist/**/*.test.js",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "start": "node dist/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^25.3.5",
    "typescript": "^5.9.3"
  }
}
